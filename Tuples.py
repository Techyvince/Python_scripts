# A tuples is a type of data structure. It is a container where we can store different value.
# A tuple is almost similar to a list.
#A tuple cant be changed or modified
coordinates = (2, 5)
locations = [(2,3), (4,5), (9,0)] #created a list of tuples using the sqare brackets
print(coordinates[1])
print(coordinates[0])
print(locations)